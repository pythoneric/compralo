[pytest]
testpaths = .
addopts = -ra --strict-markers --tb=short -q
markers =
    external: hits a real third-party service (NHTSA, etc). Skip in offline runs with `-m "not external"`.
    web: exercises the Next.js dev server (slower; needs both API and web up).
    smoke: minimal happy-path coverage; run first in CI.
